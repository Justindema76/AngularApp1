import { Component } from "@angular/core";

@Component({
  selector: "app-root",
  template: `<reservations></reservations>`, // Use the selector for ReservationsComponent
})
export class AppComponent {}
